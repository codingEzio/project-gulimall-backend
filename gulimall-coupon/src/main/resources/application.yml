spring:
  application:
    name: gulimallcoupon
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/gulimall_sms_salepromo
    driver-class-name: com.mysql.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr=127.0.0.1:8848
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
server:
  port: 7000